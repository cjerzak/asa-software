% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow__run_task.R
\name{run_direct_task}
\alias{run_direct_task}
\title{Run a Direct Provider Task Without ASA Tooling}
\usage{
run_direct_task(
  prompt,
  output_format = "text",
  config = NULL,
  agent = NULL,
  verbose = FALSE
)
}
\arguments{
\item{prompt}{The prompt or question to send directly to the configured LLM}

\item{output_format}{Expected output format. One of:
\itemize{
  \item "text": Returns response text (default)
  \item "json": Parse response as JSON
  \item "raw": Include the raw provider response object for debugging
  \item Character vector: Extract specific fields from response
}}

\item{config}{An \code{asa_config} object for unified configuration, or NULL
to use the currently initialized agent.}

\item{agent}{An \code{asa_agent} object from \code{\link{initialize_agent}},
or NULL to initialize/reuse one from \code{config}.}

\item{verbose}{Print progress messages (default: FALSE)}
}
\value{
An \code{asa_result} object with \code{execution$mode =
  "provider_direct"} and \code{search_tier = "none"}.
}
\description{
Invokes the configured provider directly using the underlying chat model
attached to an initialized agent. This bypasses the ASA agent loop, tools,
search, and temporal prompt augmentation while preserving the same backend
and model selection.
}
